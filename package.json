{
  "name": "websockets-chat-room",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "server.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:client": "tsc public/index.ts --target ES2022 --module ES2022 --lib DOM,ES2022 --skipLibCheck",
    "build:server": "tsc server.ts --target ES2022 --module ES2022 --lib ES2022,DOM --skipLibCheck",
    "build": "npm run build:client && npm run build:server",
    "dev": "npm run build:client && tsx watch server.ts",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ws": "^8.14.2"
  },
  "devDependencies": {
    "@types/node": "^20.8.0",
    "@types/ws": "^8.5.6",
    "tsx": "^4.7.1",
    "typescript": "^5.2.2"
  }
}
